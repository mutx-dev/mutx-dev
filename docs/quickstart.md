# Quickstart

The canonical quickstart now lives at [Deployment Quickstart](./deployment/quickstart.md).

Use that guide for both supported lanes:

* `Hosted operator`
* `Local contributor`

Both lanes now end with the same assistant-first outcome:

1. authenticate
2. deploy `Personal Assistant`
3. inspect it with `mutx doctor`, `mutx assistant overview`, and `mutx tui`
