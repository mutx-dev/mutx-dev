## Summary
- add `mutx deploy versions` to list deployment version history from `/v1/deployments/{id}/versions`
- add `mutx deploy rollback --version <n>` to restore a prior deployment version via `/v1/deployments/{id}/rollback`
- extend the Python SDK with deployment versions + rollback helpers and refresh deployments docs to match the live `/v1` contract
- add CLI webhook operator commands and refresh webhook docs / contract coverage so the documented operator surface matches the live CLI
- add focused CLI and SDK contract coverage for the deployment + webhook lifecycle paths touched in this lane

## Validation
- `python3 -m pytest tests/test_cli_deploy_contract.py tests/test_sdk_deployments_contract.py tests/test_cli_webhooks_contract.py -q`

## Notes
- This lane is still a clean backend/operator-contract slice from `backend-executor/deployments-versions-rollback-parity`.
- I did not claim full-repo validation; only the targeted contract suite above was run in this worktree.
- Current GitHub `Validation` failures are repo-wide format drift in unrelated files (`src/api/main.py`, two migration files, `src/api/services/monitoring.py`) plus formatting needed in the touched deployment files, not a claim that broader integration is green.
