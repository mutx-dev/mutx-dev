# MUTX UI reminder check — 2026-03-19 04:18 Europe/Rome

## What landed on `mutx-dev/mutx-dev` `main` since the previous check

Nothing landed on `main` in this interval.

- Previous reminder already had `origin/main` at `cb576257448b659c59dbd2dd89f9479d507fe6a0`
- Fresh fetch confirms `origin/main` is still `cb576257448b659c59dbd2dd89f9479d507fe6a0`
- Therefore: **no MUTX UI work landed on `main` since the previous check**

Latest commits on `main` remain:
- `cb576257` — `feat(ci): add test coverage thresholds check (#1090)`
- `338dd7fe` — `feat(runtime): add agent execution timeout enforcement (#1084)`
- `63a58923` — `ui: make app host the canonical dashboard surface`

## What is still only on branches

### Stale UI branch residue (`factory/ui-porting`)
Still only on the old UI port branch and not landed on current `main`:
- `0c07532` — `ui: port monitoring shell`
- `fec64ec` — `ui: port operator section shell`
- `57d4a12` — `ui: port dashboard shell`
- `b48306f` — `ui: port empty states`

This lane is stale/diverged and based on the older replication path; it should be selectively replayed onto canonical `/dashboard` surfaces, not blindly merged.

### Active backend/UI-unblocker branch work
Backend executor branch still has branch-only work not on `main`:
- `03266fd` — `Add deployment versions and rollback client parity`
- `6a04e9f` — `cli: add webhook operator commands`
- `b943679` — `test: restore shared async fixture imports`
- `660c445` — `cli: add agent metrics and config commands`

PR status:
- `#1183` — `feat(cli,sdk): expand operator contract parity`
- Branch: `backend-executor/deployments-versions-rollback-parity`
- State: `BLOCKED`
- Current failures: `Validation` and `Coverage Check`

## Truthful worker / blocker state

Since nothing landed, the exact active/blocked truth is:

- **No live dedicated UI worker is active.** `MUTX UI Executor v1` is still absent from the live scheduler and has no role log.
- **Backend unblocker is not active either.** `MUTX Backend Executor v1` was disabled earlier by Self-Healer after poisoned red cycles and has not landed a fix to `main`.
- The concrete frontend symptom remains the same: local preview still shows unauthenticated `401`-driven error banners on `/dashboard/agents` and `/dashboard/deployments`.
- Fresh logs captured in this reminder match that reality: the prior failed exec showed `GET /api/dashboard/agents 401` and `GET /api/dashboard/deployments 401`.

## Fresh screenshots captured

Captured from fresh local preview (`npm run dev` on `~/mutx-worktrees/factory/live-main`) at:
- `/dashboard`
- `/dashboard/monitoring`
- `/dashboard/agents`
- `/dashboard/deployments`

Files:
- `reports/ui-reminders/screenshots/2026-03-19-0418-dashboard.png`
- `reports/ui-reminders/screenshots/2026-03-19-0418-monitoring.png`
- `reports/ui-reminders/screenshots/2026-03-19-0418-agents.png`
- `reports/ui-reminders/screenshots/2026-03-19-0418-deployments.png`

## Visual status notes

- `/dashboard`: auth-gated overview shell is rendering cleanly with the control-plane frame.
- `/dashboard/monitoring`: truthful monitoring shell still renders correctly.
- `/dashboard/agents`: surface renders, but shows `Error: [object Object]` banner under unauthenticated 401 conditions.
- `/dashboard/deployments`: surface renders, but shows `[object Object]` error banner under unauthenticated 401 conditions.

## Bottom line

No UI work landed on `main` since the last check.

What is blocked:
- stale branch-only UI residue on `factory/ui-porting`
- backend/UI-unblocker work on PR `#1183`
- no active dedicated UI lane in the scheduler
- unresolved auth/error-handling truth on agents/deployments surfaces
