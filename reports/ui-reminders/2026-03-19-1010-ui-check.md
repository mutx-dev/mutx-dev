# MUTX UI Reminder Check — 2026-03-19 10:10 Europe/Rome

## Landed on `mutx-dev/mutx-dev` `main` since the previous check
Fresh fetch from `~/mutx-worktrees/factory/live-main` shows `origin/main` advanced from the prior checked head (`014cd9bd` / `ui: make dashboard chrome truthful`) to:

1. `53860335` — `ui: align website spec with canonical dashboard`

No additional newer UI commits landed after `53860335` by this check.

## What those landed changes mean
- The website/dashboard contract was tightened again so future work inherits the canonical `/dashboard` direction instead of drifting back toward older split-brain or borrowed-control-plane language.
- The current landed direct-main chain on top of the earlier convergence work is now:
  - `4d960204` — `ui: make dashboard the canonical operator surface`
  - `5902c4ca` — `ui: remove final borrowed monitoring copy`
  - `e98fbecb` — `ui: restore mutx website control-plane framing`
  - `b9d84cb8` — `ui: recover dashboard session on deployments bootstrap`
  - `7780095d` — `ui: harden dashboard auth and payload states`
  - `014cd9bd` — `ui: make dashboard chrome truthful`
  - `53860335` — `ui: align website spec with canonical dashboard`

## Still only on branches / not landed on `main`
### Older stale UI residue
- `factory/ui-porting` still represents older/superseded port residue from the pre-direct-main era (empty states, dashboard shell, operator section shell, old monitoring shell). It is not the source of truth anymore.

### Branch-only live/UI-adjacent work still not on `main`
- `fix/sdk-api-contract-alignment`
  - `ade42f5` — `ui: wire live observability routes`
  - plus the earlier high-fidelity shell replication work from that lane
- Those changes remain branch-only because truthful full-build validation was blocked by repo-level instability (OOM / edge compiler termination / unrelated type issues), so they were not honestly shipped.

## Active or blocked worker truth
- `MUTX UI Executor v1` is the active lane that produced the direct-to-main dashboard/website truth passes reflected above.
- `MUTX Backend Executor v1` remains blocked/disabled after repeated timeout failures; it is not currently carrying backend/dashboard contract follow-through.

## Fresh screenshots captured this check
Captured from local preview (`http://127.0.0.1:3000`) on current `origin/main`:
- `/dashboard` — canonical operator overview with truthful MUTX chrome
- `/dashboard/monitoring` — truthful monitoring shell
- `/dashboard/deployments` — live API-backed deployment surface with operator-session-required state

Files were captured via browser screenshots during this reminder run.

## Visual state summary
- Canonical `/dashboard` shell is live and MUTX-branded.
- Monitoring shell remains present and truthfully staged rather than fake-populated.
- Deployments surface now presents clearer runtime/auth boundaries instead of the earlier broken `[object Object]` style failure experience.
- The biggest remaining UI gap is still richer control-mutx visual/observability depth beyond the now-clean canonical shell: more dense operational panels, traces/logs/timelines, and additional visualization surfaces still need selective replay/porting from the branch-only execution lanes.
