# MUTX UI reminder check — 2026-03-19 09:39 Europe/Rome

## What landed on `mutx-dev/mutx-dev` `main` since the previous check

Fresh fetch confirms a strong new UI burst landed on `main` since the prior 08:57 reminder.

### New UI work landed on `main`
- `4d960204` — `ui: make dashboard the canonical operator surface`
- `5902c4ca` — `ui: remove final borrowed monitoring copy`
- `e98fbecb` — `ui: restore mutx website control-plane framing`

This is materially meaningful, not cosmetic churn:
- `/dashboard` is now the active richer operator shell instead of the old split-brain auth-gated dead-end surface.
- borrowed monitoring copy was explicitly removed.
- visible branding/copy shifted back toward MUTX control-plane framing instead of the earlier cloned identity drift.

### Recent `main` head stack now
- `e98fbecb` — `ui: restore mutx website control-plane framing`
- `5902c4ca` — `ui: remove final borrowed monitoring copy`
- `4d960204` — `ui: make dashboard the canonical operator surface`
- `bfd5d981` — `ui: normalize dashboard api error handling`
- `2e682807` — `fix(auth): enforce ownership on all agent endpoints (#1132)`

## What is still only on branches

### Stale UI branch residue (`factory/ui-porting`)
Still branch-only and not landed on current `main`:
- `0c07532` — `ui: port monitoring shell`
- `fec64ec` — `ui: port operator section shell`
- `57d4a12` — `ui: port dashboard shell`
- `b48306f` — `ui: port empty states`

This is now even more clearly stale residue because newer direct-to-main dashboard work has overtaken it.

### Backend/UI-unblocker branch work
Still only on backend executor branch:
- `03266fd` — `Add deployment versions and rollback client parity`
- `6a04e9f` — `cli: add webhook operator commands`
- `b943679` — `test: restore shared async fixture imports`
- `660c445` — `cli: add agent metrics and config commands`

These remain branch-only backend/operator parity work and still matter for making the dashboard more truthful over time.

## Exact worker truth

Current scheduler truth is active, not stalled:
- `MUTX UI Executor v1` — **running**
- `MUTX Shipper v2` — **running**
- `MUTX Frontend Route Unifier v1` — **running**
- `MUTX Frontend Identity Restorer v1` — recently **ok** after a run
- `MUTX Dashboard Data Hardener v1` — currently **idle** (has not produced a run yet this cadence window)
- `MUTX Release Verifier v1` — currently **idle** (has not produced a run yet this cadence window)

So the website lanes are now doing real work, and several new UI commits landed this interval.

## Fresh screenshots captured

Captured again from fresh local preview on current `origin/main`:
- `/dashboard`
- `/dashboard/monitoring`
- `/dashboard/agents`
- `/dashboard/deployments`

Files:
- `reports/ui-reminders/screenshots/2026-03-19-0939-dashboard.png`
- `reports/ui-reminders/screenshots/2026-03-19-0939-monitoring.png`
- `reports/ui-reminders/screenshots/2026-03-19-0939-agents.png`
- `reports/ui-reminders/screenshots/2026-03-19-0939-deployments.png`

## Visual status notes

### Clear improvements
- `/dashboard` now shows a real MUTX-branded operator shell instead of the previous standalone auth-required dead-end card.
- `Mission Control` branding appears to be gone from the visible dashboard shell; header/shell now read **MUTX** and **MUTX control plane** language.
- `/dashboard/monitoring` now reads as a MUTX observability surface and no longer shows the earlier obviously borrowed monitoring copy.
- `/dashboard/agents` now renders inside the same canonical dashboard shell, with readable unauthorized handling instead of raw object dumps.

### Remaining real gaps
- `/dashboard/deployments` is still not demo-safe: local preview hit a runtime error overlay showing **`Unauthorized`** thrown from `components/app/http.ts` via `DeploymentsPageClient.tsx`.
- `/dashboard` still carries some placeholder/demo framing language (`shell ready`, `demo walkthrough`, `no operator session established yet`) even though the branding is now much more MUTX-specific.
- The product is far more coherent than last check, but not fully at the acceptance bar yet.

## Bottom line

Since the previous check:
- **three new UI commits landed on `main`**
  - `4d960204` — canonical dashboard surface
  - `5902c4ca` — remove borrowed monitoring copy
  - `e98fbecb` — restore MUTX control-plane framing
- **the duplicate/split-brain dashboard problem is materially improved**
- **visible copied identity drift is materially reduced**
- **MUTX branding is now visible in the dashboard shell**
- **the deployments route still has a live unauthorized/runtime crash problem**, so the site is improved but not yet fully demo-safe
