# 2026-03-28

- Audited MUD prod after conflicting subagent/session outputs.
- Confirmed live cal.com Friday split is working: adult Friday excludes 16:00, kids Friday points to `sexta-feira-infantil`, workshops remain separate.
- Confirmed MUD prod was missing the shipped booking-payment routes and `/api/pix/intent` despite local code existing.
- Patched `src/config/booking.ts` so the Friday kids slot deep-links to `https://cal.com/mud-a7feqm/sexta-feira-infantil?...` instead of the generic Friday page.
- Committed and pushed `d59bcdb` (`Ship booking payment flow and fix Friday kids booking link`) to `origin/main` in `/Users/fortune/MUD`.
- Railway auto-deployed commit successfully (`525159dd-db79-436a-82f1-d6f9d108eba8`).
- Set Railway prod vars `PIX_PROVIDER=manual`, `PIX_MANUAL_KEY=info@mudescola.com`, and `PIX_PROOF_EMAIL=info@mudescola.com`; follow-up deploy succeeded (`8aaeead3-9806-4d1f-893e-0530a5e89b94`).
- Current prod truth: booking/payment pages + `/api/pix/intent` are live, Friday kids site link is fixed, but dynamic PSP Pix is still NOT live because Railway is missing `ASAAS_API_KEY` and all `PIX_PRICE_*_BRL` vars.
- Implemented and shipped a MUD-hosted custom Pix QR flow so the site no longer depends on Asaas to render QR + copia-e-cola. Added provider mode `custom`, BR Code payload generation, QR rendering via `qrcode`, localized UI labels, and docs/env updates.
- Pushed commit `51aefe3` (`Ship custom Pix QR flow for booking payments`) to `origin/main` in `/Users/fortune/MUD`.
- Set Railway vars `PIX_PROVIDER=custom`, `PIX_MERCHANT_NAME=MUD ESCOLA CERAMICA`, and `PIX_MERCHANT_CITY=RIO DE JANEIRO`.
- Verified live prod `/api/pix/intent` now returns `provider: custom`, `mode: dynamic`, payload + QR code data for adult, kids, and workshop slots even without `ASAAS_API_KEY` or `PIX_PRICE_*_BRL`.
- Verified live booking/payment pages show `Pix QR da MUD` / `MUD Pix QR`, the correct proof email `info@mudescola.com`, and an explicit amount-unconfigured warning until BRL price envs are filled.
- Rebuilt the MUD public booking UI to be mobile-first instead of the old 6-column ops board. Simplified inquiry page, cut booking copy heavily, moved cal.com into an embedded modal flow, and removed repeated per-slot Pix CTA clutter.
- Committed and pushed `381d355` (`Redesign mobile booking flow and simplify inquiry page`) from isolated worktree `/tmp/mud-mobile-booking-20260328` to `origin/main`.
- Railway auto-deploy `ced169bf-43a5-422c-91c7-a7b9a26790f2` succeeded.
- Verified live prod `/pt/inscricao` reflects the simplified mobile-first structure and the per-card Pix slop is gone.
- Follow-up mobile booking fix after Fortune flagged the cal.com popup as unusable on phones: rebuilt the slot modal to be full-screen on mobile, raised it above the sticky header/dock, removed the Pix CTA from inside the popup, and shrank the mobile header + dock footprint.
- Committed and pushed `a131dbf` (`Fix mobile booking modal height and simplify popup flow`) from `/tmp/mud-mobile-booking-20260328` to `origin/main`.
- Railway auto-deploy `2855607d-e5cd-4f31-99ce-35e0a9e47ec7` succeeded.
- OpenClaw local memory embeddings recovered after the broken `node-llama-cpp` install was fixed and the gateway restarted; `memory_search` now reports `provider: local` with the local GGUF embedding model.
- Hardened the memory stack to be truly local-first: `provider=local`, `fallback=none`, `onSessionStart=true`, 5-minute background sync, supported GGUF pinned via `hf:` URI, hybrid search retuned to `vectorWeight=0.7` / `textWeight=0.3`, plus MMR + temporal decay.
- Forced fresh reindex passes for the `main` and `codex` memory DBs; both completed successfully and `openclaw memory status` shows vector search available with the local 768-dim embedding model.
- Slimmed `HEARTBEAT.md` from ~26.7k to ~2.5k and `MEMORY.md` from ~21.4k to ~5.3k so bootstrap injection stops truncating those files.
- Archived the previous detailed `MEMORY.md` snapshot to `memory/mutx-reference-2026-03-24.md` so the detail remains searchable without bloating bootstrap context.
- Fortune explicitly wants **no remote embedding fallback** for memory/RAG because OpenAI embeddings have been too costly; local-only embeddings are now the standing preference.
- OpenClaw memory is currently hardened around the builtin local stack: `provider=local`, `fallback=none`, GGUF embedding model pinned via `hf:` URI, and SQLite vector search pinned to the known-good `sqlite-vec` dylib at `/opt/homebrew/lib/node_modules/openclaw/node_modules/sqlite-vec-darwin-arm64/vec0.dylib`.
- Verified local memory status after the hardening pass: `main` and `codex` reindexed cleanly, vector search is available, and the local embedding model is running at 768 dimensions.
- X agent state remains `DEGRADED`: reply/engagement cron activity appears stalled since 2026-03-25 10:30 UTC even though `browser_health=CLEAR` and `rate_limit_clear=true`.
- OpenClaw control-plane hygiene cleanup completed: backed up `openclaw.json`, `cron/jobs.json`, and `sessions.json`; pruned the cron store from 39 jobs down to the 2 live X jobs; replaced the bad `workspace-x/skills/browser-use` symlink with a real in-root copy; and cut the gateway over to a single LaunchAgent-managed process.
- Current gateway/service state after cleanup: `openclaw gateway status` reports `LaunchAgent (loaded)` and the live listener is the service-owned `openclaw-gateway` PID on port `127.0.0.1:18789`.
- The two surviving live cron jobs are `MUTX X Draft Engine v1` (`aada85e8-6d92-4018-9a74-dad30cbb6425`) and `MUTX X Worker Controller v1` (`386af6d6-4dfa-4e5b-ae2d-c89eaf600949`), both pinned to `openai-codex/gpt-5.4` with `thinking: xhigh`.
- QMD is not installed on this machine yet (`qmd` binary absent). The current official local memory stack remains builtin SQLite + `sqlite-vec` with local GGUF embeddings; QMD remains the next deeper memory/RAG upgrade rather than a prerequisite for ACP/Chrome lane stability.
- Installed QMD globally as `@tobilu/qmd` at `/opt/homebrew/bin/qmd`, enabled `memory.backend=qmd`, and built the main-agent QMD index at `~/.openclaw/agents/main/qmd/xdg-cache/qmd/index.sqlite` using local Metal acceleration on the M4.
- Fixed the QMD/OpenClaw integration enough for live assistant recall: `memory_search` now returns `provider: qmd` for repo-memory and session-memory queries, including hits from the clean MUTX markdown corpus and session transcripts.
- Built a clean git-tracked MUTX markdown mirror at `workspace/mutx-md-corpus/` (127 tracked `.md` files) so both builtin local memory and QMD index the real repo docs without vendored `.venv` noise.
- Rewrote the core bootstrap/identity files (`BOOTSTRAP.md`, `AGENTS.md`, `SOUL.md`, `IDENTITY.md`, `USER.md`, `TOOLS.md`, `HEARTBEAT.md`, `MEMORY.md`) into a leaner local-memory-first operating set aligned to revenue, trust, and anti-drift.
- Applied a local OpenClaw hotfix in the installed gateway bundle so QMD startup warmup only targets the default agent instead of stampeding every configured agent on restart.
- Expanded the main-agent QMD operating corpus from 219 to 291 files by adding workspace `reports/`, `notes/`, `MUTX RECOVERY PACKET/`, and a visible mirror of the hidden MUTX `.github` markdown docs that QMD globbing would otherwise skip.
- Main-agent QMD status after the load pass: `764` files indexed, `1713` vectors embedded, with collection contexts added for `memory-root-main` and `sessions-main` to improve ranking.
- Verified live assistant recall from the newly loaded sources: `memory_search` now returns `provider: qmd` hits from the visible `.github` MUTX agent docs (for example `mutx-github-visible/agents/mutx-webhooks.md`) and recovery-packet docs.
- Expanded the builtin local memory fallback index too: `extraPaths` now include `reports`, `notes`, `MUTX RECOVERY PACKET`, and the visible mirror of the hidden MUTX `.github` docs so both memory backends carry the same high-signal context.
- Audited the main-agent QMD corpus for duplication: found `0` duplicate relative paths (so no overwrites) but `27` duplicate-content groups from mirrored `.github` docs and exact-content `docs/` vs `mutx/docs/` copies.
- Deduped the main curated QMD corpus in place by content hash, keeping one canonical copy per document; `memory-root-main` dropped from `291` to `280` files and the corpus now reports `0` duplicate-content groups while live `memory_search` still returns `provider: qmd`.
- Ran a second-pass quality curation on the main QMD operating corpus (source files untouched): removed low-signal/repetitive material including `reports/ui-reminders`, `reports/company`, `reports/obol`, one unrelated `notes` file, and two large low-signal memory logs (`2026-03-18-agent-logs-bug.md`, `2026-03-17-spawn-codex-agents.md`).
- After the trim, `memory-root-main` dropped from `280` to `236` files; the remaining top-level mix is `mutx=111`, `sessions=33`, `memory=29`, `docs=25`, `mutx-github-visible=16`, `core=8`, `reports=8`, `recovery-packet=6`.
- Verified no quality regression after the trim: `memory_search` still returns `provider: qmd` for core MUTX positioning, hidden `.github` agent-doc queries, and recovery-packet queries.
- Trimmed the builtin local fallback index to match the same high-signal philosophy as QMD: removed broad `reports/`, `notes/`, and the dependency on the QMD-corpus mirror, replacing them with workspace-owned `reports-curated/` and `mutx-github-visible/` mirrors plus the existing `mutx-md-corpus`, core files, docs, and recovery packet.
- Verified the post-trim live recall still works cleanly after restart: `memory_search` remains `provider: qmd` for hidden `.github` agent-doc queries and recovery-packet queries, with no new `qmd query failed`, builtin-fallback, or local-embedding errors in the immediate log tail.
- Promoted 14 mission-ready MUTX agents into `workspace/mutx-agents/`, rewrote each agent's core files (`AGENTS.md`, `SOUL.md`, `IDENTITY.md`, `USER.md`, `TOOLS.md`, `HEARTBEAT.md`, `BOOTSTRAP.md`, `MEMORY.md`, `LANE.md`), and pointed their OpenClaw `workspace` fields at the new promoted directories.
- Activated and validated all promoted Wave 1 + Wave 2 lanes by direct subagent runs; every promoted agent successfully wrote real `reports/latest.md` and `queue/TODAY.md` artifacts in its workspace.
- Refreshed stale company-control truth: live GitHub status now confirms issues `#117`, `#39`, and `#114` are already closed, so `autonomy-queue.json` and `mutx-fleet-state.md` were rewritten around post-close truth audits instead of treating those issues as open work.
- Added `lane-scorecard.md` for Project Shepherd, a fresh executive `daily-brief.md` for Report Distribution Agent, and `RECOVERY-LANES.md` documenting two bounded Codex recovery lanes.
- Launched two clean recovery worktrees and direct `acpx` Codex sessions: `oc-codex-issue-117-parity` in `/Users/fortune/mutx-worktrees/recovery/issue-117-parity` and `oc-codex-issue-39-runtime-truth` in `/Users/fortune/mutx-worktrees/recovery/issue-39-runtime-truth` for post-close truth audits.
- Cost-control shift applied fleet-wide: all promoted MUTX agents now default to `gpt-5.4-mini` with `thinkingDefault=medium`, and all recurring MUTX + X cron jobs now use `payload.model=gpt-5.4-mini` and `payload.thinking=medium`.
- Honest autonomy audit on 2026-03-28: the fleet is operational and lane-capable, but not `100% autonomous with zero idle time`; direct promoted-agent execution is proven, recurring cron coverage is configured, but full always-busy zero-idle operation is neither validated nor desirable until post-close truth audits finish and runtime/product claims are reconciled.
- MUTX local onboarding docs now point operators at `http://localhost:3000/dashboard` instead of the legacy `/app` shell; `docs/deployment/local-developer-bootstrap.md` is the canonical local bootstrap path note for that surface.
- Shipped autonomy-dispatch hardening in workspace commit `4df3c9a` (`ops: make autonomy dispatch event-driven`): dispatcher now triggers on issue/PR/executor events plus cron backstop, counts autonomy-only claims/PRs with slot-based free capacity, routes long work into a new `autonomous-executor.yml` workflow, and `scripts/autonomy/build_work_order.py` now de-dupes duplicate/stale/post-close issue shapes before selection.
- Re-enabled the X reply lane with cron job `ab2e9681-40f8-4342-be36-894730752210` (`MUTX X Reply Sniper v5`), restarted the OpenClaw browser control plane after the first run exposed `running=false/cdpReady=false`, and the next live pass posted a reply on Alan Edgett’s agentic control plane thread at `https://x.com/mutxdev/status/2037977130676437365`.
- Installed Agent Reach locally via `pipx` plus OpenClaw skill registration (`~/.openclaw/skills/agent-reach`). Current state: `10/16` channels active. Best-fit decision for MUTX: use Agent Reach as an outside-in market/operator intelligence layer (X/Reddit/GitHub/YouTube/Web discovery feeding existing GTM/content/product agents), not as the primary public posting or core control-plane execution path.
- Wired the live outside-in sensing lane: created `mutx-agents/gtm/outside-in-intelligence/`, added cron job `996120e8-d629-482d-b80f-f3e080a064e6` (`MUTX Outside-In Intelligence v1`) on `20 8,13,18 * * *`, and updated the social-media, outbound, account-strategy, developer-advocate, product-manager, and report-distribution cron prompts to read the shared `reports/signal-brief.md` output.
- First live outside-in brief landed successfully after removing memory-file writes from the cron. Standing market signal: public operator discourse is converging on agent control planes, runtime policy, approval hooks, observability, and identity/permissioning as production requirements rather than optional extras.
- Ran an isolated Hermes Agent eval at `~/.hermes-eval` and migrated OpenClaw into `~/.hermes-eval/home`; persona, memory, skills, and basic messaging settings came across, but the real orchestration graph did not.
- Hermes migration archived rather than recreated the live control plane: `173` agents, `39` cron jobs across `25` agents, routing bindings, session config, and deep gateway/tool/memory-backend settings all need manual rebuild.
- Staged a lean Hermes orchestration config (explicit workspace cwd, Europe/Rome timezone, slim CLI/Discord toolsets, smart routing/delegation to `gpt-5.4-mini`), but the eval is not runnable 24/7 yet because the direct OpenAI lane in this environment is quota-exhausted and Hermes did not emerge with working Codex auth.
- Current verdict on Hermes for MUTX internal ops: interesting for shadow evaluation, not ready for live cutover. See `notes/hermes-eval-2026-03-28.md`.
- Rewired the live MUTX control/business/engineering cron graph for lower token waste and cleaner async roundtable behavior: added shared `mutx-agents` roundtable/context files, patched promoted fleet `BOOTSTRAP.md` files, and rewrote the live cron prompts to lean on `BOOTSTRAP.md` + small shared briefs instead of rereading identity/context redundantly.
- Scheduled a one-shot 8:00 Europe/Rome Sunday report sweep back to Fortune’s Discord DM across the substantive fleet (`project-shepherd`, product/build/GTM lanes, report-distribution, outside-in, the full engineering roster, plus the X system). Rule for every report: be transparent and real; if nothing material was done, say so plainly.
- Registered a real OpenClaw `outside-in-intelligence` agent and fixed the live `MUTX Outside-In Intelligence v1` cron to target that agent instead of piggybacking on `social-media-strategist`.
- Aligned the core engineering fleet cadence to the intended staggered low-idle schedule (`mission-control` 3x/hour; most specialists every 2h; auth/infra/docs every 4h), downshifted PR watcher to `15,45 * * * *`, and kept Chrome slots but shortened their prompts.
- Verified the rewired OpenClaw graph still has `39` enabled jobs; manual runs for Project Shepherd and the engineering PR watcher enqueued cleanly. Hermes shadow test also passed for scheduler control-plane operations (create/list/remove), but Hermes remains blocked for real 24/7 execution by missing working model auth.
- See `notes/orchestration-rewire-2026-03-28.md` for the full rewire summary.
