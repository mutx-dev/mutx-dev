# 2026-03-26

- Fortune wants the real `openai-codex/gpt-5.4` as the global default model, with `minimax-portal/MiniMax-M2.7` as the only fallback, and prefers reduced-noise / standby behavior from main.
- Agent catalog audit: OpenClaw config currently has 163 agents total; 156 are bulk-installed `agency-agents` templates, 147 have empty agent dirs, and most are prompt/persona shells rather than real operating agents.
- Recommended operating shape for MUTX: keep a small always-on core (`main`, `codex`, `opencode`, `x`, `outreach`, `personal-assistant`) plus a short on-call distribution/coordination bench (e.g. `developer-advocate`, `growth-hacker`, `content-creator`, `social-media-strategist`, `outbound-strategist`, `sales-engineer`, `analytics-reporter`, `technical-writer`, `account-strategist`, `brand-guardian`, `product-manager`, `workflow-architect`); prune/archive the long tail.
- Fortune’s intended direction: make the system feel like a real org with engineering pods (frontend/backend/QA/PM) and distribution pods (X/outreach delegating to writer/growth/devrel-style agents), with main orchestrating at the top instead of keeping ~160 pseudo-employees alive.
- ACP/Codex lane repair work on 2026-03-26: added a real top-level `acp` block to `~/.openclaw/openclaw.json` (enabled, backend `acpx`, defaultAgent `codex`, allowedAgents including `codex` and `opencode`), and corrected global model fallback config to MiniMax-only.
- ACP backend root cause found: plugin-local `acpx` backend had not been installed where OpenClaw expected, and the `plugins.entries.acpx.config` override was stale, pointing at `/opt/homebrew/lib/node_modules/openclaw/extensions/acpx/node_modules/.bin/acpx` with expectedVersion `0.1.16`.
- ACP repair applied: installed `acpx@0.3.1` under `/opt/homebrew/lib/node_modules/openclaw/dist/extensions/acpx/node_modules/.bin/acpx`, updated `plugins.entries.acpx.config.command` to that `dist/extensions/acpx` path, updated `expectedVersion` to `0.3.1`, and restarted the OpenClaw gateway. Live ACP spawn verification was in progress when compaction flush triggered.
- OpenClaw still emits a stale config warning for `plugins.entries.minimax-portal-auth` (`plugin not found: minimax-portal-auth`); safe cleanup candidate in config.
- X worker state on 2026-03-26: repeatedly DEGRADED, not BLOCKED — browser CLEAR, no rate limits, crons resumed, but no verified browser-side action since the last logged reply at 2026-03-25 10:30 UTC. Keep X in reduced-frequency standby until a real browser action is verified.
