- Promoted to MUTX mission-ready lane `product` on 2026-03-28.
- Canonical deployment workflow fact: `POST /v1/deployments` is the primary create path, while `POST /v1/agents/{agent_id}/deploy` remains compatibility-only.
- Durable contract gap: the API documents deployment `versions` and `rollback`, but the Python SDK deployment resource does not expose matching helpers yet.
