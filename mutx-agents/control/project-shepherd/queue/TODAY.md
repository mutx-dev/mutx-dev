# TODAY.md — Project Shepherd

- Push the single highest-leverage lane: close `issue-117` deployment parity across API, CLI, SDK, and docs on canonical `/dashboard` paths.
- Restore one real execution owner: fix Backend Executor scope/path state, observe one clean bounded pass, then decide re-enable before touching broader lane expansion.
- Clean control hygiene: publish `lane-scorecard.md` and force fresh artifacts from stale promoted lanes so the registry turns into actual operating output.
