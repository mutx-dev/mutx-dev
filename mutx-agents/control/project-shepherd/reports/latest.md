# latest.md — Project Shepherd

- Shipping truth is mixed: the queue is real and prioritized, but execution coverage is still weak because Backend Executor is blocked/quarantined and the UI executor is still disabled.
- The highest-leverage product move is still contract truth on canonical `/dashboard`: close deployment/API/CLI/SDK/docs parity drift (`issue-117`) before adding more surface area.
- Control is not clean yet: `lane-scorecard.md` is still missing, and blank/stale lane artifacts remain the main operating-system gap.
