# TODAY.md — Report Distribution Agent

## Next moves
- Turn the lane consensus into `reports/daily-brief.md`: `/dashboard` is canonical, `issue-117` is the next company move, and `issue-39` stays the gating risk.
- Refresh reporting hygiene around stale signal: flag blank/stale lane artifacts and the older shared fleet-state snapshot so Fortune gets current truth, not mixed timestamps.
- Track one clean bounded executor re-entry as the operating unlock; until that exists, keep monitoring/self-healing language conservative in every brief.
