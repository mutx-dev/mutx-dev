# TODAY.md — Sales Engineer

## Next 3 moves
1. Tighten the operator-trust demo around the supported `/dashboard` shell and explicit `/control/*` preview boundary, with CLI fallback steps called out where the UI is still thin.
2. Package governance as a CLI-first proof block (`mutx governance status`, `decisions`, `pending`, `metrics`) so buyers see the approval path and the Prometheus evidence without any fake dashboard promise.
3. Keep the SDK objection sheet pinned to the current truth: `MutxAsyncClient` is deprecated and limited, while `MutxClient` is the production recommendation.
