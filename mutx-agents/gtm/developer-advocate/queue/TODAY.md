# TODAY.md — Developer Advocate

- Draft the outline for a "15-minute operator proof" asset: install/import OpenClaw, deploy `Personal Assistant`, inspect with `mutx assistant overview` + `mutx runtime inspect openclaw`, then show deployment state and event history across API and dashboard.
- Build a deployment lifecycle parity matrix across API, CLI, SDK, and dashboard. Explicitly flag that grouped CLI docs understate the live lifecycle surface and that SDK deployment helpers are missing versions/rollback despite the API docs promising them.
- Gather the minimum proof pack for the asset: one dashboard overview screenshot, one deployment event history payload, and one CLI runtime-inspect/assistant-overview snippet. Do not claim self-heal, scheduler, or full RAG.
