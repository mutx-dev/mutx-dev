# TODAY.md — Developer Advocate

- Draft the outline for a “runtime posture / who changed what?” proof asset: install or import OpenClaw, deploy `Personal Assistant`, inspect with `mutx assistant overview` + `mutx runtime inspect openclaw`, then show deployment state and event history across API and dashboard.
- Build a compact posture matrix across API, CLI, SDK, and dashboard. Flag the gap that API docs advertise versions/rollback while the SDK deployment client still stops at logs/metrics, and keep the asset on supported lifecycle surfaces only.
- Gather the minimum proof pack for the asset: one dashboard overview screenshot, one deployment event history payload, one CLI runtime-inspect / assistant-overview snippet, and one deployment status example. Avoid claims about self-heal, scheduler, or black-box autonomy.
