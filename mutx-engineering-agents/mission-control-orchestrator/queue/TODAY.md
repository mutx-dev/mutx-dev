# TODAY.md — Mission Control Orchestrator

- Move the live review work:
  - `qa-reliability-engineer` → review PR #1202 `fix(auth): bind refresh endpoint to existing refresh cookie`
  - `infra-delivery-operator` → review PR #1209 `Fix system overview CPU and memory queries`
- Treat `control-plane-steward` as handled and keep it off the active dispatch list.
- Keep `auth-identity-guardian` and `observability-sre` idle until a new owned-area signal appears.
- Keep PR #1201 and the other open PRs queued as secondary work; do not displace the active review path.
- Keep the control brief aligned with live GitHub truth and the review matrix.
