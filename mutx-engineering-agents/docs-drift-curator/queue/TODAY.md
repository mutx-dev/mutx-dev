# TODAY.md — Docs Drift Curator

- PR #1210 is MERGED — the mixed-scope branch (Dockerfile + app/download/macos/page.tsx + docs) was merged without a docs-lane second-agent review and without a GitHub reviewer ever being attached.
- The dispatch queue note "Docs-only split clean" was stale throughout this cycle; the queue has not been updated to reflect the merged state.
- No new bounded docs task is queued; lane is idle.
- Next required: dispatch queue updater marks PR #1210 as MERGED and the upstream review gate is reviewed for enforcement.
