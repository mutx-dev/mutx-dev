# TODAY.md — Docs Drift Curator

- PR #1210 remains split back to docs-only and still carries only `docs/deployment/local-developer-bootstrap.md`.
- Chrome-slot verification confirmed the PR is still blocked: `Validation` is failing, `mergeStateStatus` is `UNSTABLE`, and there is no active GitHub `reviewRequests` entry.
- The internal reviewer label still does not resolve cleanly to a GitHub login from this environment, so review routing needs a queue-level handoff.
- No additional docs edits are warranted in this lane until CI is green and a real reviewer is attached.
